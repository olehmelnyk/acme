{
  "name": "docs-fetcher",
  "version": "1.0.0",
  "description": "Generic documentation fetcher for third-party libraries",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run cli.ts",
    "fetch": "bun run cli.ts",
    "fetch:all": "bun run cli.ts --all",
    "cleanup": "rm -rf cache"
  },
  "dependencies": {
    "fs-extra": "^11.1.1",
    "minimatch": "^9.0.3",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/testing-library__jest-dom": "^6.0.0",
    "bun-types": "latest"
  }
}
